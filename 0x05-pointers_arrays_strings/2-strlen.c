#include <string.h>


/**
* _strlen - Outputs the number of string in param *s
* @s: parameter
* Return: Always 0 (Success)
*/

int _strlen(char *s)
{

	char *str = s;

	return (strlen(str));

	return (0);
}
