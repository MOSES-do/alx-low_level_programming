#include <ctype.h>

/**
* _isdigit - Function checks if parameter returns a digit
* @c: Parameter
* Return: Always 0 (Success)
*/


int _isdigit(int c)
{
	if (isdigit(c))
		return (1);
	else
		return (0);

return (0);
}
